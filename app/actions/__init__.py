# actions/
# Phase 1 of the "actions" capability — PROPOSE ONLY, never execute.
#
# This package builds structured, stored, editable ActionProposal objects from
# indexed content. Nothing here performs an external side effect: no Microsoft
# Graph writes, no email sending, no Teams posting. Real execution is Phase 2.
