# ── Web framework ─────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart

# ── HTTP clients ──────────────────────────────────────────────────────────────
requests
httpx

# ── Storage ───────────────────────────────────────────────────────────────────
lancedb
tantivy

# ── Document extraction ───────────────────────────────────────────────────────
kreuzberg          # NO [all] — that pulls easyocr → torch (79MB)
pytesseract        # Python wrapper around system Tesseract binary
Pillow             # image handling for OCR
pdf2image          # scanned PDF → images for Tesseract

# ── AI / LLM ──────────────────────────────────────────────────────────────────
langchain-core
langchain-openai
langchain

# ── Config / utils ────────────────────────────────────────────────────────────
pyyaml
psutil
numpy
python-dotenv

libsql-client>=0.3.0
redis[asyncio]>=5.0.0
